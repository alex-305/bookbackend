package db

func postReview(username, content string, rating int) {
	//query := `INSERT INTO reviews(username,content, rating) VALUES($1, $2, $3);`
	//_, err := db.Exec(query, username, content, rating)
}
